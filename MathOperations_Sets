# Python Operations
# Math Set Operation


first = {1, 2, 3, 4, 5, 6}
second = {4, 5, 6, 7, 8, 9}

print(first | second)  # Union of two sets
print(first & second)  # Intersection gets items ONLY in both sets
print(first - second)  # Difference gets items in first set, but not in second 
print(second - first)  # Difference gets items in second set, but not in first
print(first ^ second)  # Symmetric difference get items in either set, but not both
